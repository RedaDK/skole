package opg3;

import java.util.Arrays;

public class ArrayTest {
	/*
	 * public static int sum(int[] t ) {
	 * 
	 * t = new int [] {4,6,7,2,3}; int sum = 0; for (int i : t) { sum += i; }
	 * 
	 * return sum; }
	 */
	/*
	 * public static double sumDouble(double[] t ) {
	 * 
	 * t = new double [] {2.3,2.6,7.2,2.9,3.145}; int sumDouble = 0; for (double i :
	 * t) { sumDouble += i; }
	 * 
	 * return sumDouble; }
	 */
	public int[] sumArrays(int[] a, int[] b) {
			
			int[] sum = new int [a.length];
			for(int i = 0; i < a.length; i++) {
					sum[i] = a[i] + b[i];
					System.out.println(sum[i]);
				}
			return sum;
			}

		public int[] unequal(int[] c, int[] d) {
								
			int[] sum = null;
			if(c.length < d.length) {
				sum = new int [d.length];
				for(int i = 0; i < d.length; i++) {
					
					sum[i] = c[i] + d[i];
				System.out.println(sum[i]); /*
					}
			}else if(d.length < c.length){
					sum = new int [c.length];
					for(int j = 0; j < c.length; j++) {
					
					sum[j] = d[j] + c[j];
					System.out.println(sum[j]);
			}
				*/
			}
			return sum;
		
		}
}
}
